apiVersion: v1
kind: Service
metadata:
  name: {{ include "ask-aithena-agent-chart.serviceName" . }}
  namespace: {{ include "ask-aithena-agent-chart.namespace" . }}
  labels:
    {{- include "ask-aithena-agent-chart.labels" . | nindent 4 }}
  {{- with .Values.commonAnnotations }}
  annotations:
    {{- toYaml . | nindent 4 }}
  {{- end }}
spec:
  type: {{ .Values.service.type }}
  ports:
  - port: {{ .Values.service.port }}
    targetPort: {{ .Values.service.targetPort }}
    {{- if and (eq .Values.service.type "NodePort") .Values.service.nodePort }}
    nodePort: {{ .Values.service.nodePort }}
    {{- end }}
    protocol: TCP
    name: {{ .Values.service.name }}
  selector:
    {{- include "ask-aithena-agent-chart.selectorLabels" . | nindent 4 }}
