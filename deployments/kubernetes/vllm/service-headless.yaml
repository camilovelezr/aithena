apiVersion: v1
kind: Service
metadata:
  name: vllm-nemotron-headless
  namespace: box
spec:
  clusterIP: None
  selector:
    app: vllm-nemotron
  ports:
  - port: 8000
    targetPort: 8000
    protocol: TCP
    name: http
